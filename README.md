# Phoenix Monorepo

This workspace contains the API, web, and mobile clients along with shared packages.

## Environment Files

- `.env` and `.env.preview` use the same environment variable keys.
- Use `.env.production` for production values.
