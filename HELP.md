To add a bun package to an app or package in this monorepo, you can structure your command as follows:

```bash
bun add --cwd=<path-to-app-or-pkg> <package-name>
// bun add --cwd=apps/api/ --dev nodemon
```
